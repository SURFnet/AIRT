# example mailfilter for AIRT import
# see maildropfilter(5) for syntax

logfile mailfilter.log
AIRTIMPORT=/usr/local/bin/import
MAILDIR=$HOME/Maildir

# SURFnet-CERT
if (/AIRT-importer: surfnet/:b) 
{
  cc $MAILDIR/airt-pre-filter
  xfilter $AIRTIMPORT
  to $MAILDIR/airt-post-filter
}

to $MAILDIR/Inbox
