$Id$
In CVS at $Source$

debug.php is a simple script that will dump the state of all session variables
to standard out. To install the plugin, simple copy it to a location which 
can be accessed by your web server and add a URL to it using the AIRT main 
menu URL link option.
